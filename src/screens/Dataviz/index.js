import React from 'react';

import LayoutWrapper from 'sharedUI/LayoutWrapper';

const DatavizScreen = ({ route, navigation }) => (
	<LayoutWrapper screenName={route.name}>
		<></>
	</LayoutWrapper>
);

export default DatavizScreen;
